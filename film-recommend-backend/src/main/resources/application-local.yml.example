# 本地开发环境配置示例
# 1. 复制此文件为 application-local.yml
# 2. 填入你的真实密钥和密码
# 3. application-local.yml 已在 .gitignore 中，不会被提交

spring:
  datasource:
    password: 你的数据库密码

jwt:
  secret: 生成一个强随机字符串（至少256位）

tmdb:
  access-token: 你的TMDb Access Token
