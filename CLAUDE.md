# AI 开发指令

## 项目概述

**项目名称**：影荐（电影推荐工具）

**项目定位**：轻量级 Web 端电影推荐工具，帮助用户解决“选片困难”。用户可评分、写短评、标记想看，系统基于观影偏好智能推荐，并提供随机盲盒功能。

**技术栈**：
- 前端：Vue 3 + TypeScript + Vite + Element Plus + Pinia
- 后端：Spring Boot 3.x + MyBatis-Plus + MySQL 8.0
- 认证：JWT
- 外部API：TMDb API v3
- 部署：宝塔面板 / Docker Compose

**当前阶段**：单人开发 · MVP 版本（约 2~3 周完成）

**核心功能边界**（不要额外扩展）：
1. 用户登录注册（手机号/邮箱 + 密码，JWT 认证）
2. 电影浏览（热门列表、搜索、类型筛选、详情）
3. 观影记录（评分 1~5 星、短评、想看/看过标记、删除记录）
4. 智能推荐（基于用户常看类型的 Top 3 类型推荐 + 匹配度标签）
5. 随机盲盒（从推荐池中随机抽取一部）

> ⚠️ **重要**：以下功能暂不实现——社交好友、多人匹配、片单管理、跨平台数据导入导出、观影统计图表、冷门佳片挖掘、心情/场景筛选。生成代码时不要预留或扩展这些功能的接口/组件。


## 开发规范

### 通用规范
1. **优先完成可运行的功能**，再优化代码质量。项目目标是 2~3 周跑通 MVP。
2. **先确保后端接口可正常调用**（用 Postman 测试），再写前端对接。
3. **每次新增功能前**，先在本地手动测试已有功能是否被破坏。
4. **数据库字段变更**必须记录在 PRD 的数据模型章节，保持文档与代码一致。
5. **TMDb API 调用**必须做异常处理，API 不可用时返回友好提示，不暴露内部错误。
6. **前端路由守卫**：未登录用户访问“我的”页面时自动跳转登录页。
7. **所有用户输入的短评**要做长度校验（最多 500 字符），防止数据库写入失败。
8. **密码加密**统一使用 BCrypt（Spring Security 内置），不存储明文。

### 命名规范
- 后端包名：`com.film.模块名`（如 `com.film.controller`）
- 后端类名：大驼峰（如 `MovieController`、`TmdbService`）
- 后端方法名：小驼峰（如 `getPopularMovies`、`createRecord`）
- 前端组件名：大驼峰（如 `MovieCard.vue`、`RatingStars.vue`）
- 前端变量/方法名：小驼峰（如 `userStore`、`fetchMovies`）
- 数据库表名：小写下划线（如 `watch_record`）
- 数据库字段名：小写下划线（如 `user_id`、`vote_average`）

### Git 提交规范
- `feat: 新增XXX功能`
- `fix: 修复XXX问题`
- `docs: 更新PRD/技术文档`
- `style: 调整UI样式`
- `refactor: 重构XXX代码`


## 测试要求

### 后端测试
- 使用 Postman 或 Apifox 创建接口测试集合，每个接口至少包含一个成功用例
- 重点测试：登录注册、观影记录 CRUD、推荐接口、盲盒接口
- 异常场景测试：未登录访问需鉴权接口、重复评分同一部电影（应更新而非新增）、删除不存在的记录

### 前端测试
- 在 Chrome 最新版中测试所有页面
- 重点测试：登录流程、评分交互（星星悬停高亮 + 点击确认）、盲盒抽选动画、响应式布局（缩窄浏览器窗口至 375px 宽，内容不溢出）

### 联调测试
- 前端开发环境配置代理，解决跨域问题
- 至少跑通完整用户路径：注册 → 登录 → 浏览电影 → 查看详情 → 评分评论 → 查看我的记录 → 获得推荐 → 抽盲盒


## 注意事项

### 开发相关
1. **不要使用 Redis**，初期不用缓存，直接查 MySQL + TMDb API。如需缓存，使用 Spring Cache 本地缓存或等后续迭代。
2. **TMDb API Key** 放在后端 `application.yml` 中，不要硬编码在前端代码里。
3. **跨域配置**：Spring Boot 配置 `CorsConfig`，允许前端开发域名（如 `http://localhost:5173`）。
4. **前端 API 代理**：Vite 配置 proxy 将 `/api` 转发到后端 `http://localhost:8080`。
5. **新增依赖**：后端新增依赖时确认版本兼容性（Spring Boot 3.x 要求 JDK 17+）。
6. **Element Plus 按需引入**：使用自动导入插件（`unplugin-vue-components`、`unplugin-auto-import`），不要全量引入导致包体积过大。
7. **不要过度设计**：满足 PRD 定义的功能即可，不添加“以后可能用得着”的代码。
8. **不要堆砌 AI 代码**：遇到不确定的代码优先查官方文档（Vue 3 / Spring Boot / MyBatis-Plus / Element Plus）。
9. **优先百度/Google 异常堆栈**：遇到报错先复制关键堆栈搜索解决方案，不要反复向 AI 追问。

### 环境相关
10. **Java 版本**：Spring Boot 3.x 需要 JDK 17 或更高版本，请确认本机 JDK 版本。
11. **MySQL 版本**：MySQL 8.0，注意时区配置（`serverTimezone=Asia/Shanghai`）。
12. **Node 版本**：Node.js 18.x 或更高。
13. **TMDb 注册**：访问 https://www.themoviedb.org/signup 注册账号，在 API 设置中申请 Key（免费）。
14. **TMDb 图片可访问性**：国内网络可能无法直接访问 TMDb 图片域名，部署到国内服务器时需配置 Nginx 代理或使用国内镜像源（如 `https://static-mdb.v.geilijiasu.com/t/p/w500/`）。

### 部署相关
15. **部署方式**：宝塔面板最省事，一键安装 JDK、MySQL、Nginx，上传 Jar 包和前端 dist 即可。不熟悉 Docker 的话先用宝塔。
16. **端口规划**：后端 8080，前端 Nginx 监听 80/443，避免冲突。
17. **JWT Secret**：生产环境使用强随机字符串，不要使用默认值。
18. **MySQL 备份**：上线后设置每日凌晨自动备份（宝塔面板自带备份功能，一键配置）。